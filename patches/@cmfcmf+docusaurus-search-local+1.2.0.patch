diff --git a/node_modules/@cmfcmf/docusaurus-search-local/lib/server/parse.js b/node_modules/@cmfcmf/docusaurus-search-local/lib/server/parse.js
index bf41fa2..e497ca1 100644
--- a/node_modules/@cmfcmf/docusaurus-search-local/lib/server/parse.js
+++ b/node_modules/@cmfcmf/docusaurus-search-local/lib/server/parse.js
@@ -2,7 +2,8 @@
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.getDocusaurusTag = exports.html2text = void 0;
 const tslib_1 = require("tslib");
-const cheerio_1 = tslib_1.__importDefault(require("cheerio"));
+// Cheerio v1.x CommonJS does not expose a default export; require the module directly
+const cheerio = require("cheerio");
 const logger_1 = tslib_1.__importDefault(require("./logger"));
 // We insert whitespace after text from any of these tags
 const BLOCK_TAGS = [
@@ -76,7 +77,7 @@ function getText($, el) {
     return _getText($, el).replace(/\s+/g, " ").trim();
 }
 function html2text(html, type, url = "?") {
-    const $ = cheerio_1.default.load(html);
+    const $ = cheerio.load(html);
     // Remove copy buttons from code boxes
     $("div[class^=codeBlockContent_] button").remove();
     if (type === "docs") {
@@ -109,7 +110,11 @@ function html2text(html, type, url = "?") {
                 // <a class="hash-link" href="#first-header" title="Direct link to heading">#</a>
                 .not("a[aria-hidden=true], a.hash-link")
                 .text();
-            const hash = $(heading).find("a.hash-link").attr("href") || "";
+            const hashHref = $(heading).find("a.hash-link").attr("href") || "";
+            // Extract just the hash fragment (e.g., from "/path#section" get "#section")
+            const hash = hashHref.includes("#") 
+                ? "#" + hashHref.split("#").pop() 
+                : hashHref;
             let $sectionElements;
             if ($(heading).parents(".markdown").length === 0) {
                 // $(heading) is the page title
@@ -191,7 +196,7 @@ function html2text(html, type, url = "?") {
 }
 exports.html2text = html2text;
 function getDocusaurusTag(html) {
-    const $ = cheerio_1.default.load(html);
+    const $ = cheerio.load(html);
     const tag = $('meta[name="docusaurus_tag"]').attr("content");
     if (!tag || tag.length === 0) {
         throw new Error("The `docusaurus_tag` meta tag could not be found. Please make sure that your page is wrapped in the `<Layout>` component (from `@theme/Layout`). If it is, then this is a bug, please report it.");
